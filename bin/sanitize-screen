#!/bin/zsh
if busctl get-property org.freedesktop.login1 /org/freedesktop/login1 org.freedesktop.login1.Manager Docked | grep true
then
    $HOME/.screenlayout/$(hostname)/focus.sh
else
    $HOME/.screenlayout/$(hostname)/laptop.sh
fi
